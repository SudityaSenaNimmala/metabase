# =============================================================================
# REQUIRED: MongoDB Connection (ALL data is stored in MongoDB)
# =============================================================================
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/?retryWrites=true&w=majority

# =============================================================================
# OPTIONAL: Initial Metabase Connection (can also be set via Web UI)
# These will be saved to MongoDB on first run
# =============================================================================
METABASE_URL=https://your-metabase.com
METABASE_USERNAME=your-email@example.com
METABASE_PASSWORD=your-password

# =============================================================================
# NOTE: All configuration is stored in MongoDB
# =============================================================================
# - Metabase credentials
# - Source dashboard IDs
# - Dashboard collection IDs
# - Activity logs
# - Database identification results
# 
# Use the Web UI Settings page to configure everything after first run.
